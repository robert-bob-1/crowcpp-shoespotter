#ifndef SERVICE_H
#define SERVICE_H

#include <iostream>
#include "compute.h"

void recalculateHistograms() {
    try {
        // Get shoe images from Django server


    } catch (const std::exception &e) {
        std::cerr << e.what() << std::endl;

    }
}

#endif